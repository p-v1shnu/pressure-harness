"""macOS adapter — declared in v1, implemented in M9.

Deliberately unimplemented: an abstraction that has never had a second
consumer is usually the wrong abstraction, so the ports are shaped now and
filled in when there are contract tests to fill them against (PRD 14.5).
"""
